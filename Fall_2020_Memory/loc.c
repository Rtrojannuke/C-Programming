#include <stdio.h>

int main(){

    //A program to check the values in location when values haven't been initialized

    int scores[3];
    for (int i = 0; i <3; i++)
    {
     printf("%i\n", scores[i]);
    }
}